'use client';

import { Card, CardContent, CardHeader, CardTitle, CardDescription } from '@/components/ui/card';

export default function IdiomasPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Idiomas</CardTitle>
        <CardDescription>
          Gestiona los idiomas del sistema. Esta funcionalidad está en construcción.
        </CardDescription>
      </CardHeader>
      <CardContent>
        <p>Próximamente podrás gestionar los idiomas aquí.</p>
      </CardContent>
    </Card>
  );
}
