'use client';

import { Card, CardContent, CardHeader, CardTitle, CardDescription } from '@/components/ui/card';

export default function SucursalesPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Sucursales</CardTitle>
        <CardDescription>
          Administra las sucursales de la empresa. Esta funcionalidad está en construcción.
        </CardDescription>
      </CardHeader>
      <CardContent>
        <p>Próximamente podrás gestionar las sucursales aquí.</p>
      </CardContent>
    </Card>
  );
}
